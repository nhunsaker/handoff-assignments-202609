status: corrections_requested
round: 1
verified: 50 pages × 11 conditions = 550 box files, 50 result files, 0 render errors. Rendered tempertemper category/accessibility.html at 2cda2bdda6b7 (the commit 15 used; `font-display: optional`, fonts preloaded) independently, 8 fresh browser contexts each: over a local HTTP server the web font loaded 8/8 and all 959 elements were identical across the 8 renders (docHeight 40853); over plain file:// the font never loads (root-relative /assets/fonts/… resolves to the filesystem root; docHeight 41056) and again 0 jitter.
corrections:
  - C1: The 10 "jitter" pages (all tempertemper, "webfont loading race: font-display:optional") are an artifact of serving via file:// with a request-rewriting interceptor, which delays the font into the optional block period; over HTTP the page is deterministic. The README's "Shared rendering settings" (line 87, since commit 1446e30 on 2026-09-25) require a local HTTP server, never file://. Re-run all 11 conditions for the tempertemper pages over HTTP. If the sandbox blocks localhost ("Local Network Access checks"), launch Chromium with `--disable-features=LocalNetworkAccessChecks,BlockInsecurePrivateNetworkRequests` or serve on 127.0.0.1, and record what you did. Then recompute the jitter list and C1 (fonts blocked), whose C0 baseline was itself sometimes in the fallback font.
  - C2: Confirm the other 40 pages are unaffected: for each non-tempertemper site, state whether its fonts, CSS and images are root-relative and whether they loaded in C0 (fonts_check, count of failed requests). If any did not load under file://, re-run that site over HTTP too.
  - C3: The progress log says the README has no "Shared rendering settings" / "geometry schema" sections; they are at README.md lines 87 and 97. The self-defined schema differs (paths from body, unrounded boxes, index keys). Use the README schema for the re-run, or state the mapping.
notes:
  - The parallel-worker collision was handled well: invalid boxes deleted, workers restarted, documented.
  - C3 (JavaScript disabled) changing layout on 32/50 pages (median 80 elements) is a useful result and unaffected by the serving issue for pages that render their text without JS.
